# CopyCanto UAT Report — 2026-04-26

## Scope
Headless API UAT against `http://localhost:7842` after applying all bug fixes
from the `qwen3-coder:480b` audit (26 findings, 10 files patched).

## Results

### Passing

| Test | Result |
|------|--------|
| GET / (SPA frontend) | 200 |
| GET /api/db/songs | 200 — real data returned |
| GET /api/db/voices | 200 — real data returned |
| GET /api/db/covers | 200 — real data returned |
| GET /api/db/clones | 200 — real data returned |
| GET /api/jobs/:unknown | 404 — correct not-found |
| Invalid collection (artists) | 400 — correctly rejected |

### Failures / New Issues Found

| Severity | Issue | Location |
|----------|-------|----------|
| HIGH | `POST /api/db/songs` → 400 "Missing id" — no server-side UUID generation | server.ts |
| MEDIUM | `POST /api/extract/youtube` with invalid URL → 200 with null fields instead of 4xx | server.ts |
| MEDIUM | Path traversal probe hits 500 (GitHub auth) before sanitization can block — guard ordering issue | server.ts |
| LOW | Rate limit does not block burst of 6 requests to `/api/extract/youtube` | server.ts |

### Regression Introduced by Fix

| File | Issue | Resolution |
|------|-------|------------|
| src/pages/CreateCover.tsx | Model imported `react-hot-toast` (not installed), crashed Vite build | Replaced with `alert()` to match Songs/Voices pattern |

## Files Fixed (10 total)

| File | Bugs | Model Used |
|------|------|-----------|
| server.ts | 7 (2x CRITICAL, 1x HIGH, 2x MEDIUM, 2x LOW) | devstral-2:123b |
| src/services/imageService.ts | 3 (1x CRITICAL, 2x HIGH) | qwen3-coder:480b |
| src/pages/Voices.tsx | 2 (2x HIGH resource leak) | devstral-2:123b |
| src/pages/CreateCover.tsx | 2 (1x HIGH, 1x MEDIUM) | devstral-2:123b |
| src/pages/Songs.tsx | 2 (2x MEDIUM) | devstral-2:123b |
| sync_libraries.py | 2 (2x MEDIUM) | qwen3-coder:480b |
| seed_artists.py | 3 (2x HIGH, 1x MEDIUM) | qwen3-coder:480b |
| scripts/reorganize_libraries.py | 2 (2x MEDIUM) | qwen3-coder:480b |
| scripts/recover_asdis.py | 1 (1x LOW) | qwen3-coder:480b |
| scripts/fix_library_metadata.py | 2 (1x MEDIUM, 1x LOW) | devstral-2:123b |

## Recommended Next Actions

1. `server.ts` — Auto-generate UUID in `POST /api/db/:collection` when `id` absent
2. `server.ts` — Validate YouTube URL format before extraction; return 422 on bad input
3. `server.ts` — Ensure path sanitization runs before GitHub auth check in repo routes
4. `server.ts` — Apply rate limiter to `/api/extract/youtube`
5. Decide on toast pattern: install `react-hot-toast` or standardize on `alert()`
